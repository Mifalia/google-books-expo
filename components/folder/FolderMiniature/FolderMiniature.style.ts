import { StyleSheet } from "react-native";

export default StyleSheet.create({
  miniatureContainer: {
    flexDirection: "row",
    alignItems: "center",
    paddingVertical: 12,
    borderBottomWidth: 1,
    borderColor: "#e8ecef",
    gap: 16,
  },

  iconContainer: {},

  texContainer: {
    gap: 4,
  },

  folderTitle: {
    fontSize: 14,
    color: "#3F3D56",
    fontWeight: "bold",
  },

  folderSmall: {
    fontSize: 12,
    color: "#6b7280",
  },
});
